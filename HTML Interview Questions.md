1. What is HTML?
	Ans: HTML (Hyper Text Markup Language) is a markup language as the name suggests. It is used for structuring a web page 

2. What are Tags? Do all Tags have an ending tag?
	Ans: Tags are primary components of HTML that defines how the content would be sturctured. And not all HTML tags have an end tag. For example the `<br>` & `<img>` tags

3. What is the difference between HTML and HTML5?
	HTML is a markup language 

4. Can we display a web page inside a web page or Is nesting of webpages possible?
	Ans: Yes we can display a web page inside another HTML web page. with iframe tag

5. What are Attributes?
6. What is formatting 
7. How many types of heading does HTML have?
8. How to create a hyperlink in HTML?
9. What are some common lists that are used when designing a page
10. What is the difference between HTML elements and tags
11. What is semantic HTML
12. What is an image map
13. What are HTML Entities
14. What is the ‘class’ attribute in HTML