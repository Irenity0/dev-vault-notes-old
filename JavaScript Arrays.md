In JavaScript, arrays are used to store multiple values in a single variable. They are ordered collections of elements, which can be of any data type. Here’s a guide to working with arrays:

### Creating Arrays

1. **Using Array Literals**:
   ```javascript
   let fruits = ['apple', 'banana', 'cherry'];
   ```

2. **Using the `Array` Constructor**:
   ```javascript
   let numbers = new Array(1, 2, 3, 4);
   ```

### Accessing and Modifying Elements

- **Accessing Elements**:
  ```javascript
  let firstFruit = fruits[0]; // 'apple'
  ```

- **Modifying Elements**:
  ```javascript
  fruits[1] = 'blueberry'; // Changes 'banana' to 'blueberry'
  ```

Arrays are flexible and powerful tools in JavaScript for handling lists of data, and the various methods available allow for extensive manipulation and interaction with array elements.